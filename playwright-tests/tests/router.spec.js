/**
 * potlock.near/widget/Index
 */

import { test, expect } from "@playwright/test";

test("show load the default route when provided", async ({ page }) => {
  
});

test("show load the other routes when provided", async ({ page }) => {});
